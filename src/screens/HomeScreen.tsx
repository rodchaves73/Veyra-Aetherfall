import { GameAssetImage } from '../components/assets/GameAssetImage';
import { VeyraAssetFrame, VeyraButton, VeyraPanel, VeyraResourcePill, VeyraScreen } from '../components/ui/VeyraVisual';
import { VAProgressBar } from '../components/ui/VAProgressBar';
import type { ScreenId } from '../app/navigation';
import type { PlayerState } from '../lib/rpg/types';
import { heroes } from '../data/heroes';
import { gameAssets } from '../lib/assets';

export function HomeScreen({ state, onNavigate }: { state: PlayerState; onNavigate: (screen: ScreenId) => void }) {
  const spotlight = heroes[2];
  return <VeyraScreen background={gameAssets.backgrounds.home}><VeyraPanel className="min-h-64"><GameAssetImage decorative src={gameAssets.fx.portal.src} fallbackSrc={gameAssets.fx.portal.fallbackSrc} className="veyra-portal-breathe absolute left-1/2 top-7 w-52 -translate-x-1/2 opacity-50" /><div className="relative z-[1]"><p className="text-xs uppercase tracking-[.35em] text-cyan-100/75">Colete os caídos. Domine o Aether.</p><h2 className="mt-2 text-3xl font-black leading-tight">Reconstrua um mundo partido.</h2><div className="mt-4 grid grid-cols-3 gap-2 text-center text-xs"><div><b>{state.player.level}</b><p className="text-violet-100/55">Nível</p></div><div><b>{state.player.power}</b><p className="text-violet-100/55">Power</p></div><div><b>{state.campaignProgress.currentStageId}</b><p className="text-violet-100/55">Stage</p></div></div><div className="mt-4 grid grid-cols-2 gap-2"><VeyraButton className="veyra-shimmer" onClick={() => onNavigate('battle')}>Battle</VeyraButton><VeyraButton variant="gold" onClick={() => onNavigate('summon')}>Summon</VeyraButton></div></div></VeyraPanel><div className="grid grid-cols-2 gap-2"><VeyraResourcePill asset={gameAssets.icons.gold} value={state.inventory.gold} label="Gold" /><VeyraResourcePill asset={gameAssets.icons.gems} value={state.inventory.aether_shards} label="Gems" /><VeyraResourcePill asset={gameAssets.icons.stamina} value={state.inventory.stamina} label="Stamina" /><VeyraResourcePill asset={gameAssets.icons.standardTicket} value="0" label="Tickets" /></div><VeyraPanel><div className="flex items-center gap-4"><VeyraAssetFrame rarity={spotlight.rarity} className="h-24 w-24 shrink-0"><GameAssetImage src={gameAssets.heroes.placeholder.src} fallbackSrc={gameAssets.heroes.placeholder.fallbackSrc} alt="Hero placeholder" className="h-24 w-24 object-cover" /></VeyraAssetFrame><div className="min-w-0 flex-1"><p className="text-[10px] uppercase tracking-widest text-amber-200">Hero Spotlight</p><h3 className="font-black">{spotlight.name}</h3><p className="text-xs text-violet-100/65">{spotlight.shortLore}</p></div></div></VeyraPanel><VeyraPanel><GameAssetImage decorative src={gameAssets.ui.divider.src} fallbackSrc={gameAssets.ui.divider.fallbackSrc} className="mb-2 h-3 w-full object-fill opacity-70" /><VAProgressBar value={state.campaignProgress.clearedStages.length} max={10} label="Chapter 1: Ruins of the First Seal" /><div className="mt-4 grid grid-cols-2 gap-2"><VeyraButton onClick={() => onNavigate('dungeons')}>Dungeons</VeyraButton><VeyraButton variant="secondary" onClick={() => onNavigate('fountain')}>Rewards</VeyraButton></div></VeyraPanel><div className="grid grid-cols-2 gap-3"><Shortcut title="Fonte do Aether" text="Ads limitados" onClick={() => onNavigate('fountain')} /><Shortcut title="Shop" text="Packs seguros" onClick={() => onNavigate('shop')} /><Shortcut title="Heroes" text="Coleção" onClick={() => onNavigate('heroes')} /><Shortcut title="Wallet" text="TON preparado" onClick={() => onNavigate('wallet')} /></div></VeyraScreen>;
}
function Shortcut({ title, text, onClick }: { title: string; text: string; onClick: () => void }) { return <button onClick={onClick} className="veyra-arcane-panel veyra-card-hover min-h-24 p-3 text-left"><b>{title}</b><p className="mt-1 text-xs text-violet-100/60">{text}</p></button>; }
