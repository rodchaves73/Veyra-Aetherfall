import { navItems, type ScreenId } from '../../app/navigation';
export function BottomNav({ active, onChange }: { active: ScreenId; onChange: (screen: ScreenId) => void }) { return <nav className="fixed inset-x-0 bottom-0 z-40 mx-auto max-w-[430px] border-t border-white/10 bg-[#070713]/90 px-2 pb-[calc(env(safe-area-inset-bottom)+6px)] pt-2 backdrop-blur-xl"><div className="grid grid-cols-7 gap-1">{navItems.map((item) => <button key={item.id} onClick={() => onChange(item.id)} className={`flex min-h-12 flex-col items-center justify-center rounded-2xl text-[10px] transition ${active === item.id ? 'bg-violet-500/25 text-cyan-100' : 'text-violet-100/55'}`}><item.icon size={18} /><span>{item.label}</span></button>)}</div></nav>; }
