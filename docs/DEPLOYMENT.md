# Deploy

## Vercel

- Framework Preset: Vite
- Install Command: `npm install`
- Build Command: `npm run build`
- Output Directory: `dist`

`vercel.json` reescreve todas as rotas para `index.html`, adequado para SPA. Configure variáveis VITE públicas a partir de `.env.example`. Não suba `.env` real.
